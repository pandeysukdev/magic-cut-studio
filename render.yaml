services:
  - type: web
    name: magic-cut-studio
    runtime: python
    buildCommand: |
      apt-get update && apt-get install -y libglib2.0-0 libsm6 libxext6 libxrender-dev libgomp1
      pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
